# DNC COMPUTERZ

Dee’s lot. Advertise items for sale. REDLINE first-draft, client-owned single HTML.

Drop `index.html` at the root of a new Vercel project. No build step. Tailwind CDN + anime.js 3.2.2.

Holds and contact go to dnccomputerz@gmail.com.

Clone a ticket: copy one `<article class="item ticket">` block and change SKU, title, price, condition, status. Instructions are in the HTML comment at the top of `index.html`.
